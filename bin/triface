#!/usr/bin/env bash -x

COMMAND=$1
NAME=$2

if [ $COMMAND == "new" ]
then
    mkdir -p $NAME/{app/{model,controller,view},config/migrations,public}
elif [ $COMMAND == "install" ] 
then
    echo "install install"
fi
